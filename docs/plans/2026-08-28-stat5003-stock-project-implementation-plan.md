# STAT5003 Stock Project Microsite Implementation Plan

1. Add the standalone semantic HTML shell with responsive sidebar navigation and two content panels.
2. Add the English dataset and research content, keeping provisional analytical assumptions visibly labelled.
3. Add complete Chinese and Korean translation dictionaries and the language-state controller.
4. Add hash routing, accessible active states, local language persistence, and no-JavaScript fallback behaviour.
5. Add the microsite card to the QuickSites directory page and update the README page list.
6. Run static checks and local browser interaction tests across languages, routes, reloads, and mobile viewport.
7. Review the final diff, commit with the repository decision record, push `main`, and verify GitHub Pages.

